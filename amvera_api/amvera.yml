meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"
build:
  requirementsPath: requirements.txt
run:
  command: python -m uvicorn api:app --host 0.0.0.0 --port 8000
  containerPort: 8000